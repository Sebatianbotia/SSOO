int main(){
    int nHijos;
    char buffer[100];
    printf ("ingrese el numero de hijos: ");// hola
    scanf ("%d", &nHijos); // aa
    int nPipes = nHijos+1;
    pid_t root = getpid();
    int (*pipes)[2];
    pipes = ( int (*)[])malloc(nPipes*sizeof( int [2]));
    
    for ( int j=0; j<nPipes;j++){
        if(pipe(pipes[j])==-1){
            perror("error al crear la tuberia");
            exit(1);
        }
    }
}